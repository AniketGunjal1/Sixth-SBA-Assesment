spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/studentdb?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernet.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      
     
